# 更新日志

本文档记录 `astrbot_plugin_steam_updates` 的版本变更。

## v1.2.10

### 新增
- 新增 notify_umos 配置，支持按完整 UMO 向多个平台、多个适配器实例和 AstrBot 合法消息类型主动推送
- 新旧推送目标合并后按规范 UMO 保序去重，旧 notify_group_ids 与 platform_id 继续兼容

### 修复
- 单个目标失败时继续发送其余目标，图片失败仅向对应目标补发文本
- 全部目标发送失败时保留轮询状态，避免更新在发送成功前被消费
- steam_update_ping 改为捕获平台实例 ID，避免同类型多个适配器混用

## v1.2.9

### 变更
- 新增 Steam 免费领取活动推送与手动查询，支持作为独立分区“限时免费领取”附在游戏更新后面
- 新增免费领取功能开关与手动查询回退开关，默认关闭自动推送，默认在无普通更新时仅返回免费领取分区
- 数据源使用 GamerPower 公开接口，筛选 Steam 平台游戏活动
- 免费领取活动仅展示标题、截止时间和原价；截止时间支持配置显示时区
- 新活动首次发现时主动推送一次，领取期间会随普通游戏更新附带提醒

## v1.2.8

### 优化
- 优化游戏更新推送卡片的可读性与准确性
[PR#4](https://github.com/DearCrazyLeaf/astrbot_plugin_steam_updates/pull/4) by [Rhonin Wang](https://github.com/RhoninSeiei)

### 变更
- 轮询推送时仅包含本周期内有更新的 AppID
- 游戏更新卡片补充“公告/新闻标题”

<details>
<summary>历史版本（点击展开）</summary>

## v1.2.7

### 修复
- 添加去重机制，防止同一更新多次推送
[issues#2](https://github.com/DearCrazyLeaf/astrbot_plugin_steam_updates/issues/2);[PR#3](https://github.com/DearCrazyLeaf/astrbot_plugin_steam_updates/pull/3) by [Rhonin Wang](https://github.com/RhoninSeiei)

## v1.2.6

### 变更
- 配置重新优化分组

## v1.2.5

### 变更
- 修复创意工坊手动查询回退仅显示一条的问题：当天无更新时，回退结果保留所有订阅 ID 的最新内容，不再被单日窗口折叠
- 修复卡片顶部日期显示错误：顶部日期改为查询日期，避免与“发布于”时间混淆
- 优化 LLM 超时/失败时的回退展示：自动清洗 BBCode/HTML 标记后再渲染，避免原始脏文本导致卡片难以阅读
- 优化手动查询卡片一致性：游戏更新与创意工坊更新统一按同一日期语义展示

## v1.2.4

### 变更
- 新增创意工坊查询功能，支持多ID查询，自动轮询和手动查询。
- 支持头图请求开关，优化查询性能。

## v1.2.3

### 修复
- 修复轮询推送图片时的消息组件构造问题：改为先保存临时图片文件，再使用 `Image(file=...)` 发送，避免字节流被当作文本路径解析导致的发送异常。
- 新增轮询图片推送失败后的降级策略：图片发送失败时自动回退为文本推送，避免本轮更新消息丢失。

### 优化
- 统一 `_push_text`、`_push_image`、`_push_chain` 返回值语义，明确推送成功状态，便于轮询分支做失败处理。

## v1.2.2

### 新增
- 新增 WebUI 代理配置：
- `proxy_mode`（`off` / `system` / `custom`）
- `proxy_url`（当 `proxy_mode=custom` 时生效）
- 新增 SOCKS 代理依赖：`socksio`。

### 变更
- HTTP 客户端初始化改为根据代理配置动态创建，不再固定直连。
- 补充 README 代理配置说明。

## v1.2.1

### 新增
- 新增 Steam Feed 回退配置：
- `enable_feed_fallback`
- `feed_timeout_sec`

### 变更
- 优化轮询、数据获取、手动查询、推送链路的结构化控制台日志，便于排障。

## v1.2.0

### 新增
- 新增定时轮询起始时间配置，支持从指定时间点开始按间隔执行轮询。

### 变更
- 移除旧轮询模式分支，统一为“起始时间 + 间隔”调度逻辑，降低维护复杂度。
- 补充文档中的轮询配置说明。

## v1.1.0

### 新增
- 新增卡片渲染样式优化与本地预览开发流程支持。
- 新增手动查询指令可配置能力（支持多指令）。
- 新增 LLM 内容整理模式与相关配置项。

### 变更
- 优化新闻内容清洗与分段展示逻辑，提升更新日志可读性。
- 增强 AppID 名称映射与多语言显示能力。

## v1.0.0

### 新增
- 首个可用版本发布。
- 支持基于 Steam News API 的游戏更新轮询与群推送。
- 支持多 AppID 查询、文本/卡片两种消息模式。

</details>
